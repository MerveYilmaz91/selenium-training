package DAY2;

import org.openqa.selenium.WebDriver;
import org.openqa.selenium.chrome.ChromeDriver;

/**
public class Definitions {
    public static void main(String[] args) {

        /**

        WebDriver driver = new ChromeDriver();

        driver.get("https://www.google.com"); //driver(web sürücüsü) yönlendirir

        driver.manage().window().maximize();  //ekranı büyütür
        driver.manage().window().maximize();  //ekranı alta alır
        driver.manage().window().fullscreen();   //F11 Moduna geçer (tüm sayfayı kapladı)
        driver.manage().window().getSize();  //pencerenin boy ölçülerini döndürür

        driver.getTitle();  // web in içerisinde bir bilgi alıp ekrana döndürür
        String title = driver.getTitle();

        String url = driver.getCurrentUrl("https://www.google.com/");  // Selenium’da şu an açık olan sayfanın URL’sini alıp ekrana yazdırıyor.
        System.out.println(url);

        driver.getCurrentUrl();  //browserdaki anlık url i döndürür
        driver.getPageSource();  // HTML kaynak kodlarını döndürür.

        driver.quit(); // driver ı sonlandırır
        driver.close(); // sadece sekmeyi kapatır


         **/

